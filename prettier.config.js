// @ts-check

/** @type {import('prettier').Config} */
const config = {
	trailingComma: 'all',
	semi: false,
	singleQuote: true,
	printWidth: 120,
	tabWidth: 2,
	useTabs: true,
}

export default config
